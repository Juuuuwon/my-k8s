apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: ghost
  name: ghost
spec:
  ingressClassName: nginx
  rules:
  - host: juwon.codes 
    https:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ghost
            port:
              number: 443